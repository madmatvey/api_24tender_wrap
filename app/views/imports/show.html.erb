<%- model_class = Import -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:time_to) %>:</strong></dt>
  <dd><%= @import.time_to %></dd>
  <dt><strong><%= model_class.human_attribute_name(:time_from) %>:</strong></dt>
  <dd><%= @import.time_from %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              imports_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_import_path(@import), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              import_path(@import),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
